{% extends 'common/bases/public-base.html.twig' %}

{% block header_class %} header-top {% endblock %}

{% block body %}
<section id="contact" class="contact section-show">
    <p>test test</p>
    <div class="container">
        {# <===== CONTACT-INFO =====> #}
        <div class="section-title">
            <h2>{{ 'contact.title'|trans }}</h2>
        </div>
        <div class="row mt-2">
            <div class="col-md-6 d-flex align-items-stretch">
                <div class="info-box">
                    <i class="bx bx-share-alt"></i>
                    <h3>{{ 'contact.social.sites.title'|trans }}</h3>
                    <div class="social-links">
                        <a href="{{ instagram_link }}" class="instagram" target="_blank"><i class="bi bi-instagram"></i></a>
                        <a href="{{ telegram_link }}" class="telegram" target="_blank"><i class="bi bi-telegram"></i></a>
                        <a href="{{ twitter_link }}" class="twitter" target="_blank"><i class="bi bi-twitter"></i></a>
                    </div>
                </div>
            </div>
            <div class="col-md-6 mt-4 mt-md-0 d-flex align-items-stretch">
                <div class="info-box">
                    <i class="bx bx-envelope"></i>
                    <h3>{{ 'contact.email.me.title'|trans }}</h3>
                    <p>{{ contact_email }}</p>
                </div>
            </div>
        </div>

        {# <===== CONTACT-FORM =====> #}
        <div class="msg-from mt-4">
            {{ form_start(contact_form) }}

                {% if error_msg != null %}
                    <div class="error-message">{{ error_msg|trans }}</div>
                {% endif %}
                {% if success_msg != null %}
                    <div class="sent-message">{{ success_msg|trans }}</div>
                {% endif %}

                {# SET PLACEHOLDERS FOR TRANSLATE #}
                {{ form_widget(contact_form.name, {'attr': {'placeholder': 'contact.form.name.input'|trans}}) }}
                {{ form_widget(contact_form.email, {'attr': {'placeholder': 'contact.form.email.input'|trans}}) }}
                {{ form_widget(contact_form.message, {'attr': {'placeholder': 'contact.from.message.input'|trans}}) }}
                
                {{ form_widget(contact_form) }}                     
                <div class="text-center mt-3">
                    <button type="submit">{{ 'contact.form.submit.button'|trans }}</button>
                </div>
            
            {{ form_end(contact_form) }}      
        </div>
    </div>
</section>
{% endblock %}
