{% extends 'common/bases/admin-base.html.twig' %}

{% block title %}
    database
{% endblock %}

{% block component %}
<div class="admin-panel">
    {% if user_role != 'owner' %}
        <h2 class="page-title">Sorry you dont have permission to this page</h2>
    {% else %}
        {#=================================== TABLES-LIST ==================================#}
        {% if tables != null %}
            <h2 class="page-title">Select table</h2>
            <br>
            {% for table in tables %}
                <a class='db-browser-select-link' href="/admin/database/{{table|e}}/1">{{table}}</a>
                <br><br>
            {% endfor %}
        {% else %}
            {#=================================== ROW-EDITOR ===================================#}
            {% if editor_table != null %}
            {% include "admin/elements/database-navigation.html.twig" %}
            {% if error_msg != null %}
                {% include "admin/elements/alerts/error-alert.html.twig" %}
            {% endif %}
            <center>
                <form class="db-edit-form dark-table" action="/admin/database/edit/{{editor_page|e}}/{{editor_table|e}}/{{editor_id|e}}" method="post">
                    <p class="title">Edit {{editor_table|e}}, row: {{editor_id|e}}<p></p>
                    {% for field in editor_field %}
                        {% if field != 'id' %}
                            <p class="text-input-title">{{field|e}}</p>
                            <input class="text-input" type="text" name="{{field|e}}" value="{{editor_values[field]|e}}"><br>
                        {% endif %}
                    {% endfor %}
                    <input class="input-button" type="submit" name="submitEdit" value="Edit">
                </form>
            </center>
            {% else %}
                {#================================= NEW-ROW =================================#}
                {% if new_row_table != null %}
                    {% include "admin/elements/database-navigation.html.twig" %}
                    {% if error_msg != null %}
                        {% include "admin/elements/alerts/error-alert.html.twig" %}
                    {% endif %}
                    <center>
                        <form class="db-edit-form dark-table" action="/admin/database/add/{{new_row_page|e}}/{{new_row_table|e}}" method="post">
                            <p class="title">Add new: {{new_row_table|e}}<p></p>
                            {% for field in new_row_columns %}
                                {% if field != 'id' %}
                                    <p class="text-input-title">{{field|e}}</p>
                                    <input class="text-input" type="text" name="{{field|e}}" placeholder="{{field|e}}"><br>
                                {% endif %}
                            {% endfor %}
                            <input class="input-button" type="submit" name="submitSave" value="SAVE">
                        </form>
                    </center>
                {% else %}
                    {#================================= TABLES-BROWSER =================================#}
                    {% if table_name != null %}
                        {% include "admin/elements/database-navigation.html.twig" %}
                        {% if table_data_count == 0 %}
                            <h2 class="page-title">table: '{{table_name|e}}' is empty</h2>
                        {% else %}
                            <div class="table-responsive">
                                <table class="table table-dark">
                                    <thead>
                                        <tr class="line-item">  
                                            {% for column in table_columns %}
                                                {% if column == 'id' %}
                                                    <th scope='col'>#</th>
                                                {% else %}                                                 
                                                    <th scope='col'>{{column|e}}</th>
                                                {% endif %}
                                            {% endfor %}
                                            {% if table_name == 'code_paste' %}
                                                <th cope='col'>Link</th>
                                            {% endif %}
                                            <th cope='col'>Edit</th>
                                            <th cope='col'>X</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for row in table_data %}
                                            <tr class="line-item">
                                                {% for column in table_columns %}
                                                    <th scope='row'>
                                                        {% set content = row[column] | slice(0, 200) %}
                                                        {{ content ~ (row[column] | length > 200 ? '...' : '') }}
                                                    </th>
                                                {% endfor %}
                                                {% if table_name == 'code_paste' %}
                                                    <th cope='col'><a href="/paste/view/{{row.name|e}}" target="_blank">{{row.name|e}}</a></th>
                                                {% endif %}
                                                <td><a class="text-warning delete-link-todos" href="/admin/database/edit/{{page|e}}/{{table_name|e}}/{{row.id|e}}">Edit</a></td>
                                                <td><a class="delete-link-todos" href="/admin/database/delete/{{page|e}}/{{table_name|e}}/{{row.id|e}}">X</a></td>
                                            </tr>
                                        {% endfor %} 
                                    </tbody>
                                </table>
                            </div>
                            <div class="page-button-box">
                                {% if page != 1 %}
                                    <a class="back-page-button" href="/admin/database/{{table_name|e}}/{{page|e - 1}}">Back</a>
                                {% endif %}
                                {% if limit == table_data_count %}
                                    <a class="back-page-button" href="/admin/database/{{table_name|e}}/{{page|e + 1}}">Next</a>
                                {% endif %}
                            </div>
                            {% if page != 1 or limit == table_data_count %}
                                <br>
                            {% endif %}
                        {% endif %}
                    {% endif %}
                {% endif %}
            {% endif %}
        {% endif %}
    {% endif %}
</div>
{% endblock %}
