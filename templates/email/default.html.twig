<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>becvar-site email</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #ffffff;
            padding: 20px;
            margin: 0;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
            color: rgb(75, 255, 255);
        }
        .container {
            max-width: 600px;
            margin: auto;
            background-color: #333333;
            padding: 30px;
            border-radius: 10px;
        }
        p {
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 20px;
            color: #ffffff;
        }
        .footer {
            margin-top: 30px;
            font-size: 14px;
            text-align: center;
            color: #ffffff;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{ subject }}</h1>
        </div>
        <p>{{ message }}</p>
    </div>
    <div class="footer">
        This is an automated email from becvar-site
    </div>
</body>
</html>
