<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ subject }}</title>
    <style>
        body {
            background-color: #121212;
            color: #e0e0e0;
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        .subject {
            border-bottom: 1px solid #444;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .subject h2 {
            color: rgb(255, 55, 55);
            margin: 0;
        }
        .subject h2 span {
            float: right;
            color: rgb(202, 202, 202)
        }
        .message {
            color: white;
            font-weight: bold;
        }
        .message div {
            color: white;
            position: fixed;
            bottom: 0;
            font-size: 16px;
            margin-bottom: 5px;
        }
        a {
            color: #c595ff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="subject">
        <h2>{{ subject }} <span>{{ time }}</span></h2>
    </div>
    <div class="message">
        <p>{{ message }}</p>
        {% if ip_address != 'protected' %}
            <div>ip-address: {{ ip_address }}</div>
        {% endif %}
    </div>
</body>
</html>
